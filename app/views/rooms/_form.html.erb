<%= form_for(@room) do |f| %>
  <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :projector %><br />
    <%= f.check_box :projector %>
  </div>
  <div class="field">
    <%= f.label :sound %><br />
    <%= f.check_box :sound %>
  </div>
  <div class="field">
    <%= f.label :mic %><br />
    <%= f.check_box :mic %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
